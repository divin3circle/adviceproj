import React from "react";

const AdviceTitle = ({ title }) => {
  return (
    <div>
      <h1 className="text-title text-center">Advice #{title}</h1>
    </div>
  );
};

export default AdviceTitle;
